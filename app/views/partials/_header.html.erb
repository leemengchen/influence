<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="#">Influence</a>
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-dropdown" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>

        <div class="collapse navbar-collapse" id="header-dropdown">
            <ul class="nav navbar-nav navbar-right">
              <li><a href="#">Home<span class="sr-only">(current)</span></a></li>
              <li><a href="#">About<span class="sr-only">(current)</span></a></li>

                <% if current_influencer %>
                <li><%= link_to current_influencer.email, edit_influencer_registration_path(current_influencer), class: 'white shadowed link-styling' %></li>
                <li><%= link_to "Logout", destroy_influencer_session_path, method: :delete, class: 'white shadowed link-styling' %></li>
                <% else %>
                <li><a href="#" data-toggle="modal" data-target="#login-modal">Log In<span class="sr-only">(current)</span></a></li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
